const config = {
    supportColor: '#e6b217',
    pointSize: 10,
    curvesColor: '#ff0000',
    tStep: 0.001,
    supportLineWidth: 2,
    curvesPointRadius: 0.5,
}

config.pointCoorOffset = config.pointSize / 2;

export { config };